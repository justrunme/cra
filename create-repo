#!/bin/bash

CONFIG_FILE="$HOME/.create-repo.conf"
[ -f "$CONFIG_FILE" ] && source "$CONFIG_FILE"

mkdir -p ~/.create-repo/templates

# Автоопределение платформы
if command -v gh &>/dev/null; then
  default_platform="github"
elif [ -n "$GITLAB_TOKEN" ]; then
  default_platform="gitlab"
else
  echo "❌ Не найдена платформа. Установите gh или переменную GITLAB_TOKEN"
  exit 1
fi

platform=${default_platform}
default_cron_interval=${default_cron_interval:-1}
default_visibility=${default_visibility:-public}

# ✅ Проверка зависимостей
missing=""
check_dep() {
  if ! command -v "$1" &>/dev/null; then
    echo "❌ Не найдено: $1"
    missing+=" $1"
  else
    echo "✅ Найдено: $1"
  fi
}
check_dep git
check_dep curl
check_dep crontab

if [[ "$platform" == "github" ]]; then
  check_dep gh
fi

if ! command -v notify-send &>/dev/null; then
  echo "ℹ️ Не найдено: notify-send (необязательно, для уведомлений)"
fi
if ! command -v dpkg &>/dev/null; then
  echo "ℹ️ Не найдено: dpkg (нужно для .deb-обновлений)"
fi

if [ -n "$missing" ]; then
  echo ""
  echo "⚠️ Необходимые пакеты не найдены:$missing"
  echo "💡 Установи их: sudo apt install$missing"
  exit 1
fi

# 📁 Определение репозитория
repo=${1:-$(basename "$PWD")}
echo "📁 Текущая директория: $PWD"
echo "🔤 Имя репозитория: $repo"

echo "🌐 Платформа выбрана автоматически: $platform"

# 🛠️ Инициализация Git
if [ ! -d .git ]; then
  git init
  git checkout -b main
else
  git checkout main 2>/dev/null || git checkout -b main
fi

# README
if [ ! -f README.md ]; then
  template="$HOME/.create-repo/templates/${platform}.README.md"
  if [ -f "$template" ]; then
    cp "$template" README.md
  else
    echo "# $repo" > README.md
  fi
fi

# .gitignore
if [ ! -f .gitignore ]; then
  if [ -f "$HOME/.create-repo/templates/${platform}.gitignore" ]; then
    cp "$HOME/.create-repo/templates/${platform}.gitignore" .gitignore
  else
    echo ".DS_Store" > .gitignore
  fi
fi

# Git add
git add .
NOW=$(date "+%Y-%m-%d %H:%M:%S")
git commit -m "Initial commit at $NOW" 2>/dev/null || echo "⚠️ Коммит уже был."

# 🔗 Создание удалённого репозитория и push
if [[ "$platform" == "github" ]]; then
  remote_url="git@github.com:justrunme/$repo.git"
  if ! gh repo view "$repo" &>/dev/null; then
    echo "🌐 Создаём репозиторий на GitHub..."
    gh repo create "$repo" --private --source=. --push
  fi
else
  remote_url=$(curl -s --header "PRIVATE-TOKEN: $GITLAB_TOKEN" \
    --data "name=$repo&visibility=$default_visibility" \
    https://gitlab.com/api/v4/projects | grep -oP '"ssh_url_to_repo":"\K[^"]+')
fi

# 📌 Проверка remote
if ! git remote get-url origin &>/dev/null; then
  git remote add origin "$remote_url"
else
  echo "ℹ️ Remote 'origin' уже существует, пропускаем добавление."
fi

# 🌀 Отслеживание ветки и push
git branch --set-upstream-to=origin/main main 2>/dev/null

echo "🚀 Выполняем git push..."
if git push -u origin main 2>/dev/null; then
  echo "✅ Репозиторий синхронизирован с origin/main"
else
  echo "⚠️ Не удалось выполнить push. Проверь авторизацию или доступ."
fi

# 📝 Добавление в автослежение
grep -qxF "$PWD" ~/.repo-autosync.list 2>/dev/null || echo "$PWD" >> ~/.repo-autosync.list
sort -u ~/.repo-autosync.list -o ~/.repo-autosync.list

# ⏱️ Установка cron-задачи
cron_interval=$default_cron_interval
TMP_CRON=$(mktemp)
crontab -l 2>/dev/null | grep -v 'update-all' > "$TMP_CRON"
echo "*/$cron_interval * * * * /usr/local/bin/update-all" >> "$TMP_CRON"
sort -u "$TMP_CRON" -o "$TMP_CRON"
crontab "$TMP_CRON"
rm "$TMP_CRON"
echo "✅ Cron-задача настроена на каждые $cron_interval мин"

# 🎉 Финальное сообщение
echo ""
echo "🎉 Всё готово!"
echo "📁 Путь проекта: $PWD"
echo "🌐 Платформа: $platform"
echo "📦 Репозиторий: $repo"
echo "📝 Добавлен в автослежение: ~/.repo-autosync.list"
echo "⏱️ Cron-задача: каждые $cron_interval мин"
echo ""
echo "💡 Полезные команды:"
echo "   🛠 Изменить платформу (GitHub/GitLab):"
echo "     nano ~/.create-repo.conf  # и укажи default_platform=gitlab"
echo ""
echo "   ⏱ Изменить интервал cron-синхронизации:"
echo "     nano ~/.create-repo.conf  # и измени default_cron_interval=5"
echo ""
echo "   🧹 Удалить проект из автослежения:"
echo "     sed -i '\\|$PWD|d' ~/.repo-autosync.list"
echo ""
echo "   🚫 Отключить авто-синхронизацию (cron):"
echo "     crontab -l | grep -v update-all | crontab -"
echo ""
echo "   🔁 Включить обратно:"
echo "     echo \"*/1 * * * * /usr/local/bin/update-all\" | crontab -"
echo ""
echo "📘 Документация: https://github.com/justrunme/cra"
