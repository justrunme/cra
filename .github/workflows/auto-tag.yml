name: Auto Tag and Push

description: |
  This workflow runs on every push to the main branch,
  determines the latest version tag (vX.Y.Z), increments the patch version,
  creates a new git tag, and pushes it to GitHub.

on:
  push:
    branches:
      - main

jobs:
  autotag:
    runs-on: ubuntu-latest

    steps:
      - name: 📃 Checkout repository
        uses: actions/checkout@v4

      - name: 🔢 Set up Git user
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

      - name: 🔢 Get latest tag and increment version
        id: get_tag
        shell: bash
        run: |
          latest=$(git describe --tags --abbrev=0 2>/dev/null || echo "v1.0.0")
          echo "✅ Latest tag: $latest"
          version=${latest#v}
          IFS='.' read -r major minor patch <<< "$version"
          patch=$((patch + 1))
          new_tag="v$major.$minor.$patch"
          echo "🆕 New tag: $new_tag"

          git tag "$new_tag"
          git push origin "$new_tag"

      - name: ✅ Done
        run: echo "🎉 Auto-tag completed."
